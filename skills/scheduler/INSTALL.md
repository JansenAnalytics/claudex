# Install: scheduler

This skill wraps the standalone `scheduler` project. The skill body references
the project via the `$SCHEDULER_HOME` env var, with a default of `~/projects/scheduler`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/scheduler.git ~/projects/scheduler

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export SCHEDULER_HOME=~/projects/scheduler
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${SCHEDULER_HOME:-$HOME/projects/scheduler}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${SCHEDULER_HOME:-$HOME/projects/scheduler}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
