# Install: hypothesis-tester

This skill wraps the standalone `hypothesis-tester` project. The skill body references
the project via the `$HYPOTHESIS_TESTER_HOME` env var, with a default of `~/projects/hypothesis-tester`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/hypothesis-tester.git ~/projects/hypothesis-tester

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export HYPOTHESIS_TESTER_HOME=~/projects/hypothesis-tester
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${HYPOTHESIS_TESTER_HOME:-$HOME/projects/hypothesis-tester}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${HYPOTHESIS_TESTER_HOME:-$HOME/projects/hypothesis-tester}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
