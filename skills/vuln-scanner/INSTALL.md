# Install: vuln-scanner

This skill wraps the standalone `vuln-scanner` project. The skill body references
the project via the `$VULN_SCANNER_HOME` env var, with a default of `~/projects/vuln-scanner`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/vuln-scanner.git ~/projects/vuln-scanner

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export VULN_SCANNER_HOME=~/projects/vuln-scanner
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${VULN_SCANNER_HOME:-$HOME/projects/vuln-scanner}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${VULN_SCANNER_HOME:-$HOME/projects/vuln-scanner}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
