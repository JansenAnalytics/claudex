# Install: project-planner

This skill wraps the standalone `project-planner` project. The skill body references
the project via the `$PROJECT_PLANNER_HOME` env var, with a default of `~/projects/project-planner`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/project-planner.git ~/projects/project-planner

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export PROJECT_PLANNER_HOME=~/projects/project-planner
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${PROJECT_PLANNER_HOME:-$HOME/projects/project-planner}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${PROJECT_PLANNER_HOME:-$HOME/projects/project-planner}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
