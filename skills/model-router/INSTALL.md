# Install: model-router

This skill wraps the standalone `model-router` project. The skill body references
the project via the `$MODEL_ROUTER_HOME` env var, with a default of `~/projects/model-router`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/model-router.git ~/projects/model-router

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export MODEL_ROUTER_HOME=~/projects/model-router
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${MODEL_ROUTER_HOME:-$HOME/projects/model-router}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${MODEL_ROUTER_HOME:-$HOME/projects/model-router}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
