# Install: project-brief

This skill wraps the standalone `project-brief` project. The skill body references
the project via the `$PROJECT_BRIEF_HOME` env var, with a default of `~/projects/project-brief`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/project-brief.git ~/projects/project-brief

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export PROJECT_BRIEF_HOME=~/projects/project-brief
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${PROJECT_BRIEF_HOME:-$HOME/projects/project-brief}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${PROJECT_BRIEF_HOME:-$HOME/projects/project-brief}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
