# Install: task-queue

This skill wraps the standalone `task-queue` project. The skill body references
the project via the `$TASK_QUEUE_HOME` env var, with a default of `~/projects/task-queue`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/task-queue.git ~/projects/task-queue

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export TASK_QUEUE_HOME=~/projects/task-queue
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- `$TASK_QUEUE_HOME/cli.cjs` — main entrypoint invoked by the skill

## Verification

```bash
ls "${TASK_QUEUE_HOME:-$HOME/projects/task-queue}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${TASK_QUEUE_HOME:-$HOME/projects/task-queue}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
