# Install: mcp-hub

This skill wraps the standalone `forex-factory-scraper` project. The skill body references
the project via the `$FOREX_FACTORY_SCRAPER_HOME` env var, with a default of `~/projects/forex-factory-scraper`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/forex-factory-scraper.git ~/projects/forex-factory-scraper

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export FOREX_FACTORY_SCRAPER_HOME=~/projects/forex-factory-scraper
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${FOREX_FACTORY_SCRAPER_HOME:-$HOME/projects/forex-factory-scraper}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${FOREX_FACTORY_SCRAPER_HOME:-$HOME/projects/forex-factory-scraper}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
