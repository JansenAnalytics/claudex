# Install: market-monitor

This skill wraps the standalone `market-monitor` project. The skill body references
the project via the `$MARKET_MONITOR_HOME` env var, with a default of `~/projects/market-monitor`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/market-monitor.git ~/projects/market-monitor

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export MARKET_MONITOR_HOME=~/projects/market-monitor
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${MARKET_MONITOR_HOME:-$HOME/projects/market-monitor}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${MARKET_MONITOR_HOME:-$HOME/projects/market-monitor}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
