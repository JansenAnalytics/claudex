# Install: research-reporter

This skill wraps the standalone `research-reporter` project. The skill body references
the project via the `$RESEARCH_REPORTER_HOME` env var, with a default of `~/projects/research-reporter`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/research-reporter.git ~/projects/research-reporter

# 2. Install dependencies
cd ~/projects/research-reporter && npm install

# 3. (Optional) Pin the location with an env var
export RESEARCH_REPORTER_HOME=~/projects/research-reporter
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${RESEARCH_REPORTER_HOME:-$HOME/projects/research-reporter}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${RESEARCH_REPORTER_HOME:-$HOME/projects/research-reporter}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
