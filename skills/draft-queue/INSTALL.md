# Install: draft-queue

This skill wraps the standalone `draft-queue` project. The skill body references
the project via the `$DRAFT_QUEUE_HOME` env var, with a default of `~/projects/draft-queue`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/draft-queue.git ~/projects/draft-queue

# 2. Install dependencies
cd ~/projects/draft-queue && npm install

# 3. (Optional) Pin the location with an env var
export DRAFT_QUEUE_HOME=~/projects/draft-queue
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${DRAFT_QUEUE_HOME:-$HOME/projects/draft-queue}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${DRAFT_QUEUE_HOME:-$HOME/projects/draft-queue}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
