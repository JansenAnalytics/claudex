# Install: source-scorer

This skill wraps the standalone `source-scorer` project. The skill body references
the project via the `$SOURCE_SCORER_HOME` env var, with a default of `~/projects/source-scorer`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/source-scorer.git ~/projects/source-scorer

# 2. Install dependencies
cd ~/projects/source-scorer && npm install

# 3. (Optional) Pin the location with an env var
export SOURCE_SCORER_HOME=~/projects/source-scorer
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${SOURCE_SCORER_HOME:-$HOME/projects/source-scorer}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${SOURCE_SCORER_HOME:-$HOME/projects/source-scorer}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
