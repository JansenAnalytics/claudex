# Install: secret-vault

This skill wraps the standalone `secret-vault` project. The skill body references
the project via the `$SECRET_VAULT_HOME` env var, with a default of `~/projects/secret-vault`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/secret-vault.git ~/projects/secret-vault

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export SECRET_VAULT_HOME=~/projects/secret-vault
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${SECRET_VAULT_HOME:-$HOME/projects/secret-vault}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${SECRET_VAULT_HOME:-$HOME/projects/secret-vault}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
