# Install: pr-monitor

This skill wraps the standalone `pr-monitor` project. The skill body references
the project via the `$PR_MONITOR_HOME` env var, with a default of `~/projects/pr-monitor`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/pr-monitor.git ~/projects/pr-monitor

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export PR_MONITOR_HOME=~/projects/pr-monitor
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${PR_MONITOR_HOME:-$HOME/projects/pr-monitor}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${PR_MONITOR_HOME:-$HOME/projects/pr-monitor}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
