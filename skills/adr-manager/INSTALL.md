# Install: adr-manager

This skill wraps the standalone `adr-manager` project. The skill body references
the project via the `$ADR_MANAGER_HOME` env var, with a default of `~/projects/adr-manager`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/adr-manager.git ~/projects/adr-manager

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export ADR_MANAGER_HOME=~/projects/adr-manager
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${ADR_MANAGER_HOME:-$HOME/projects/adr-manager}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${ADR_MANAGER_HOME:-$HOME/projects/adr-manager}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
