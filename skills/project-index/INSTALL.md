# Install: project-index

This skill wraps the standalone `project-index` project. The skill body references
the project via the `$PROJECT_INDEX_HOME` env var, with a default of `~/projects/project-index`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/project-index.git ~/projects/project-index

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export PROJECT_INDEX_HOME=~/projects/project-index
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- `$PROJECT_INDEX_HOME/index.cjs` — main entrypoint invoked by the skill

## Verification

```bash
ls "${PROJECT_INDEX_HOME:-$HOME/projects/project-index}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${PROJECT_INDEX_HOME:-$HOME/projects/project-index}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
