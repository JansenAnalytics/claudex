# Install: rss-digest

This skill wraps the standalone `rss-digest` project. The skill body references
the project via the `$RSS_DIGEST_HOME` env var, with a default of `~/projects/rss-digest`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/rss-digest.git ~/projects/rss-digest

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export RSS_DIGEST_HOME=~/projects/rss-digest
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${RSS_DIGEST_HOME:-$HOME/projects/rss-digest}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${RSS_DIGEST_HOME:-$HOME/projects/rss-digest}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
