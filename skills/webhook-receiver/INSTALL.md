# Install: webhook-receiver

This skill wraps the standalone `webhook-receiver` project. The skill body references
the project via the `$WEBHOOK_RECEIVER_HOME` env var, with a default of `~/projects/webhook-receiver`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/<your-github-org>/webhook-receiver.git ~/projects/webhook-receiver

# 2. Install dependencies
# (no dependency manifest detected — check the project's README)

# 3. (Optional) Pin the location with an env var
export WEBHOOK_RECEIVER_HOME=~/projects/webhook-receiver
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${WEBHOOK_RECEIVER_HOME:-$HOME/projects/webhook-receiver}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${WEBHOOK_RECEIVER_HOME:-$HOME/projects/webhook-receiver}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
