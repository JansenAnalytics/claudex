# Claudex Skills Catalog

_Auto-generated by `scripts/skill-index.sh` — do not edit by hand._

**161 skills** across **14 categories** (0 private 🔒, excluded from the public showcase repo).

## Summary

| Category | Count | Covers |
|----------|------:|--------|
| [`development`](#development) | 19 | Generic dev workflow — git, CI/CD, code review, testing, refactoring |
| [`frontend`](#frontend) | 11 | UI/UX — React, CSS, accessibility, responsive & visual review |
| [`backend`](#backend) | 6 | Server-side — APIs, databases, schema, mocking, perf profiling |
| [`security`](#security) | 7 | Security audits, vuln scanning, secret & credential management |
| [`research`](#research) | 12 | Web/deep research, monitoring, doc reading, hypothesis testing |
| [`data`](#data) | 7 | Data engineering & analysis — ETL, SQL, validation, visualization |
| [`system`](#system) | 16 | OS/infra admin — services, backups, docker, logs, networking |
| [`comms`](#comms) | 9 | Person-to-person messaging — Slack, email, push, voice |
| [`writing`](#writing) | 6 | Authoring — docs, knowledge bases, ADRs, drafts, prompts |
| [`productivity`](#productivity) | 18 | Personal org — calendars, notes, tasks, scheduling, clipboards |
| [`media`](#media) | 14 | Image/video/audio — generation, transcription, OCR, screenshots |
| [`trading-finance`](#trading-finance) | 11 | Markets & money — market data, TA, sentiment, ordering, prop trading |
| [`home-iot`](#home-iot) | 8 | Smart home & local devices — lights, speakers, weather, places |
| [`meta`](#meta) | 17 | Self-tooling — skill/model management, MCP, LLM CLIs |
| **Total** | **161** | |

**Maturity:** stable 132, beta 29

---

## development

_Generic dev workflow — git, CI/CD, code review, testing, refactoring_ — 19 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **arch-diagram** | stable | Generate architecture diagrams from codebase analysis: Mermaid, PlantUML, or ASCII. | `mermaid`, `plantuml`, `ascii-diagram`, `dependency-graph`, `architecture` |
| **architecture-critic** | stable | Static code analysis like a senior developer — cyclomatic/cognitive complexity, dead code detection, dependency graph analysis (circular deps, god modules), pattern consistency, error handling audit, code duplication detection. Use during refactors or before shipping. | `static-analysis`, `complexity`, `dead-code`, `dependency-graph`, `duplication` |
| **ci-cd** | stable | Create and debug GitHub Actions CI/CD pipelines: workflow authoring, matrix builds, caching, secrets, deployment steps. | `github-actions`, `workflows`, `matrix-builds`, `caching`, `deployment` |
| **code-review** | beta | Review code for bugs, improvements, and best practices. Use when asked to review a file, PR, or codebase. | `pr-review`, `gh-cli`, `code-quality`, `bug-detection`, `review-checklist` |
| **codebase-navigator** | stable | Rapidly understand any codebase: identify entry points, map architecture, find key patterns, summarize modules, trace data flow. | `code-navigation`, `dependency-map`, `entry-points`, `ripgrep`, `data-flow-trace` |
| **coding-agent** | stable | Delegate coding tasks to Codex, Claude Code, or Pi agents via background process. Use when: (1) building/creating new features or apps, (2) reviewing PRs (spawn in temp dir), (3) refactoring large codebases, (4) iterative coding that needs file exploration. NOT for: simple one-liner fixes (just edit), reading code (use read tool), or any work in ~/clawd workspace (never spawn agents here). Requires a bash tool that supports pty:true. | `codex`, `claude-code`, `agent-delegation`, `background-process`, `git-worktree` |
| **dep-audit** | stable | Audit and update project dependencies: find outdated packages, check for vulnerabilities, auto-update safely with lockfile regeneration. | `dependencies`, `npm`, `pip`, `lockfile`, `outdated` |
| **doc-generator** | stable | Auto-generate documentation from code: README files, JSDoc comments, API docs. Scans project structure, detects frameworks, and produces ready-to-use documentation. | `readme`, `jsdoc`, `api-docs`, `code-scanning`, `documentation` |
| **gh-issues** | stable | Fetch GitHub issues, spawn sub-agents to implement fixes and open PRs, then monitor and address PR review comments. Usage: /gh-issues [owner/repo] [--label bug] [--limit 5] [--milestone v1.0] [--assignee @me] [--fork user/repo] [--watch] [--interval 5] [--reviews-only] [--cron] [--dry-run] [--model glm-5] [--notify-channel <your-telegram-channel-id>] | `github-api`, `issue-triage`, `pr-automation`, `sub-agents`, `code-review-handler` |
| **git-bisect-auto** | stable | Automated git bisect to find the exact commit that introduced a bug. Run a test command against each bisect step, report the first bad commit with full context. | `git-bisect`, `regression-finding`, `binary-search`, `test-runner`, `regression-report` |
| **github-workflow** | stable | Git and GitHub operations — clone, branch, commit, push, PRs, issues, CI status. Use for any git or GitHub task. | `gh-cli`, `git`, `pull-requests`, `issues`, `ci-status` |
| **migration-planner** | stable | Plan and execute framework, language, or infrastructure migrations safely: version upgrades, framework swaps, API changes. | `migration`, `upgrade`, `breaking-changes`, `rollback`, `checklist` |
| **pipeline-runner** | stable | Execute multi-step pipelines defined in JSON — sequential and parallel stages, dependency chains, conditionals, retries with backoff, per-step timeouts, variable passing, and run history. Use when running or orchestrating a multi-step JSON-defined pipeline. | `pipeline`, `workflow-orchestration`, `dependency-graph`, `retries`, `json` |
| **pr-monitor** | stable | Monitor GitHub repo health — CI failures on main, PRs awaiting review over 24h, PRs with failing checks, stale PRs — and alert. Use when checking repo/PR/CI health, investigating why an alert fired, or adding a repo to watch. | `github`, `ci-monitoring`, `pull-requests`, `cron`, `alerts` |
| **project-scaffold** | stable | Scaffold new projects from templates: Node.js, Python, React, API server, CLI tool, monorepo. | `scaffolding`, `boilerplate`, `templates`, `config-generation`, `project-setup` |
| **refactor** | stable | Large-scale codebase restructuring: rename, extract, inline, move modules, update imports. Use when: restructuring project layout, extracting components/functions, renaming across files, splitting ... | `dead-code`, `import-updater`, `dependency-graph`, `code-restructuring`, `monolith-splitting` |
| **release-manager** | stable | Manage releases: semantic versioning, changelog generation, GitHub releases, git tags, version bumping. | `semver`, `changelog`, `git-tags`, `github-releases`, `version-bump` |
| **test-critic** | stable | Evaluates test quality — coverage analysis, mutation testing (do tests actually catch bugs?), test gap detection, and test code quality scoring. Supports Jest, Vitest, pytest. Use after writing tests to verify they're meaningful. | `test-quality`, `mutation-testing`, `coverage`, `jest`, `pytest` |
| **test-runner** | stable | Run and interpret test suites across frameworks: Jest, Vitest, pytest, Mocha, tap. Use when: running tests, debugging test failures, generating coverage reports, writing test fixtures, or setting u... | `test-execution`, `coverage`, `flaky-tests`, `framework-detection`, `pytest` |

## frontend

_UI/UX — React, CSS, accessibility, responsive & visual review_ — 11 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **a11y-audit** | stable | Accessibility auditing: WCAG compliance checking, contrast ratios, ARIA labels, keyboard navigation, semantic HTML, screen reader compatibility. | `accessibility`, `wcag`, `aria`, `contrast-ratio`, `keyboard-nav` |
| **design-critic** | stable | Autonomous UI/UX review — screenshots every page at mobile/tablet/desktop (full-page scrolling), analyzes with vision models for layout bugs, checks accessibility (axe-core), audits contrast/colors/fonts/spacing for design system inconsistencies, detects dead buttons/links, overlapping elements hiding text/buttons, text truncation, broken links, and runs Lighthouse performance audits. Produces a prioritized report with severity scores and actionable fixes. Use before any launch, after major UI changes, or when the user asks to check/review a web app. | `ui-review`, `screenshots`, `accessibility`, `axe-core`, `lighthouse` |
| **design-system** | stable | Enforce design consistency: component inventory, style token extraction, CSS consistency checking, spacing/color/typography auditing. | `design-tokens`, `css`, `component-inventory`, `style-audit`, `consistency` |
| **e2e-test-writer** | stable | Generate end-to-end tests for web applications using Playwright. Create test files from user flow descriptions, page snapshots, or existing page analysis. | `playwright`, `e2e-tests`, `user-flows`, `test-generation`, `web-ui` |
| **flow-tester** | stable | End-to-end user journey tester using Playwright. Tests multi-step flows (signup, checkout, CRUD) with screenshots at each step. Auto-generates flows from URLs or text descriptions. Use after integration work to verify complete user journeys work. | `playwright`, `e2e`, `user-journeys`, `browser-automation`, `screenshots` |
| **frontend-design** | stable | Based on Anthropic's Frontend Aesthetics research. Adapted for OpenClaw with practical tooling additions. | `aesthetics`, `css`, `typography`, `design-direction`, `ui-design` |
| **frontend-inspector** | stable | Analyze frontend screenshots using vision model analysis and automated checks: detect layout bugs, alignment issues, overflow, z-index problems, spacing inconsistencies, color contrast issues. | `screenshot-analysis`, `css-linting`, `layout-bugs`, `vision-model`, `visual-review` |
| **lighthouse-perf** | stable | Run performance audits on web pages: Lighthouse scores, Core Web Vitals, resource analysis, bundle size tracking, performance budgets. | `lighthouse`, `core-web-vitals`, `performance-budget`, `resource-analysis`, `web-page` |
| **react-frontend** | stable | Opinionated guide for building data-heavy React applications — dashboards, trading UIs, analytics tools. Based on the 2025/2026 React ecosystem and our actual stack (React 19 + Vite + TypeScript + ... | `react`, `vite`, `typescript`, `tailwind`, `dashboards` |
| **responsive-checker** | stable | Test responsive design across viewport sizes: capture screenshots at mobile/tablet/desktop breakpoints, detect overflow issues, flag breakpoint problems. | `responsive`, `breakpoints`, `overflow-detection`, `screenshots`, `media-queries` |
| **visual-review** | stable | Screenshot comparison and visual regression detection: pixel diffing, before/after overlays, threshold-based change detection. | `visual-regression`, `screenshot-diff`, `pixel-diffing`, `overlay`, `ui-testing` |

## backend

_Server-side — APIs, databases, schema, mocking, perf profiling_ — 6 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **api-critic** | stable | Autonomous API testing and evaluation. Tests any REST API for correctness, security, performance, error handling, and standards compliance. Discovers endpoints, probes with valid/invalid/edge-case payloads, checks auth, response times, injection vulnerabilities, and generates severity-scored reports with actionable fixes. Use before any API "done" claim. | `rest-api`, `endpoint-testing`, `injection-testing`, `auth`, `severity-scoring` |
| **api-tester** | stable | Systematic REST and GraphQL API endpoint testing: happy path, error cases, auth flows, rate limits, schema validation. Use when: testing backend APIs, validating endpoint responses, checking error ... | `rest-api`, `graphql`, `endpoint-testing`, `auth-flows`, `schema-validation` |
| **database** | stable | Manage PostgreSQL, SQLite, and Redis databases: create, migrate, backup, restore, query, optimize. | `postgresql`, `sqlite`, `redis`, `migration`, `backup` |
| **mock-server** | stable | Spin up mock API servers for frontend development and testing: static responses, record/replay real API calls, dynamic response generation. | `mock-api`, `record-replay`, `openapi`, `rest`, `stubbing` |
| **perf-profiler** | stable | Runtime performance analysis beyond Lighthouse — bundle size analysis, memory leak detection, HTTP load testing, resource optimization audit, and runtime CPU profiling. Use before launch or when investigating performance issues. | `load-testing`, `bundle-size`, `memory-leak`, `cpu-profiling`, `playwright` |
| **schema-analyzer** | stable | Analyze database schemas: generate ERD diagrams, detect normalization issues, suggest indexes, find missing foreign keys, audit schema quality. | `erd`, `schema-audit`, `indexes`, `foreign-keys`, `normalization` |

## security

_Security audits, vuln scanning, secret & credential management_ — 7 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **1password** | stable | Set up and use 1Password CLI (op). Use when installing the CLI, enabling desktop app integration, signing in (single or multi-account), or reading/injecting/running secrets via op. | `1password`, `op-cli`, `secrets`, `credential-vault`, `signin` |
| **healthcheck** | stable | Host security hardening and risk-tolerance configuration for OpenClaw deployments. Use when a user asks for security audits, firewall/SSH/update hardening, risk posture, exposure review, OpenClaw cron scheduling for periodic checks, or version status checks on a machine running OpenClaw (laptop, workstation, Pi, VPS). | `hardening`, `firewall`, `ssh`, `risk-posture`, `openclaw-audit` |
| **secret-vault** | stable | Encrypted credential store — AES-256-GCM secured tokens and secrets in one place. | `aes-256-gcm`, `credential-store`, `encryption`, `cli`, `node` |
| **secrets-manager** | stable | Manage secrets and environment variables securely: .env files, encrypted vaults, key rotation, secret scanning. Use when: setting up environment variables, managing API keys, rotating secrets, scan... | `dotenv`, `secret-scanning`, `key-rotation`, `credentials`, `leak-detection` |
| **security-audit** | stable | Security auditing: vulnerability scanning, dependency checking, port scanning, permission review. Use when: auditing project security, scanning for CVEs in dependencies, checking open ports, review... | `vulnerability-scan`, `port-scan`, `dependency-audit`, `permission-audit`, `http-headers` |
| **security-auditor** | stable | Automated security testing — HTTP header audit, injection testing (SQL/XSS/SSRF/command), secret scanning in source code, auth bypass testing, dependency vulnerability scanning. Pen-test-style checks with safe mode default. Use before any launch or security review. | `pentest`, `injection-testing`, `secret-scanning`, `auth-bypass`, `cvss` |
| **vuln-scanner** | beta | Scan project dependencies for known security vulnerabilities (npm-audit style) across your projects and alert. Use when checking projects for vulnerabilities, after adding a new npm project, or reviewing the last scan results. | `vulnerability-scanning`, `npm-audit`, `cve`, `cron`, `alerts` |

## research

_Web/deep research, monitoring, doc reading, hypothesis testing_ — 12 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **blogwatcher** | beta | Monitor blogs and RSS/Atom feeds for updates using the blogwatcher CLI. | `rss`, `atom`, `feed-monitoring`, `blogs`, `updates` |
| **deep-research** | beta | Conduct thorough multi-source research on any topic. Use when the user needs in-depth analysis, comparison, or investigation of a subject. | `multi-source`, `web-search`, `wikipedia`, `synthesis`, `cross-reference` |
| **doc-reader** | stable | PDF/DOCX/XLSX/image document intelligence — text extraction, table parsing, OCR, financial statement analysis, contract clause detection, document classification, and format conversion. Use when reading, analyzing, extracting data from, or converting documents. | `pdf-extraction`, `ocr`, `table-parsing`, `docx`, `document-intelligence` |
| **doc-verifier** | stable | Ensures documentation is accurate, complete, and actually works — catches documentation rot before users hit it. Validates code examples, checks links, compares API/CLI docs against reality, detects stale docs, and scores completeness. | `doc-validation`, `link-check`, `freshness`, `completeness-score`, `example-runner` |
| **hypothesis-tester** | stable | Run a structured multi-position debate over an analytical question, spawning sub-agents to argue each option and counter confirmation bias. Use when comparing options or tradeoffs — 'which is better', 'should we', 'what's the risk', 'evaluate', 'compare options'. | `debate`, `tradeoff-analysis`, `subagents`, `synthesis`, `decision-support` |
| **market-monitor** | stable | Watch any URL for meaningful changes — price moves, new releases (semver), hiring signals, content diffs, keyword appear/disappear — and alert. Use when tracking competitors, releases, or job postings, or detecting content changes on a web page. | `url-monitoring`, `content-diff`, `release-watch`, `hiring-signal`, `cron` |
| **research-reporter** | stable | Collect research findings (source, URL, content, confidence, tags) across sources and compile them into a structured report. Use when doing multi-source research and you want findings saved as a structured, citable report. | `findings`, `confidence-scoring`, `report-compilation`, `sessions`, `multi-source` |
| **rss-digest** | stable | Subscribe to RSS/Atom feeds with keyword filters and run digests delivered to Telegram. Use when subscribing to feeds, checking which feeds run, running a digest manually, or adjusting feed keywords. | `rss`, `feeds`, `keywords`, `digest`, `cron` |
| **source-scorer** | stable | Score the credibility of a web source by domain tier, content signals, and claim support; handles single or batch URLs. Use when judging whether a source is trustworthy, comparing sources, or validating research findings. | `credibility`, `source-scoring`, `domain-tier`, `verification`, `cli` |
| **summarize** | stable | Summarize or extract text/transcripts from URLs, podcasts, and local files (great fallback for “transcribe this YouTube/video”). | `summarization`, `transcript-extraction`, `youtube`, `url-summary`, `cli` |
| **web-monitor** | beta | Monitor websites for changes, stock availability, price drops. Use when asked to watch a URL or check for changes. | `url-watch`, `price-drop`, `stock-alert`, `cron`, `ntfy` |
| **web-search** | beta | Search the web and fetch content from URLs. Use for research, fact-checking, news, or any web lookup. | `duckduckgo`, `url-fetch`, `text-extraction`, `lynx`, `fact-checking` |

## data

_Data engineering & analysis — ETL, SQL, validation, visualization_ — 7 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **data-analysis** | beta | Analyze data from CSV, JSON, SQLite, or APIs. Use when asked to analyze, summarize, or visualize data. | `csv`, `json`, `sqlite`, `matplotlib`, `summarization` |
| **data-pad** | beta | Use when: the user provides a CSV, JSON, or NDJSON file and wants analysis, filtering, aggregation, or exploration. Also use when you have structured data from an API and want to query it. | `csv`, `ndjson`, `sql`, `sqlite`, `exploration` |
| **data-pipeline** | stable | ETL and data transformation: CSV, JSON, Parquet, NDJSON processing with filtering, mapping, joining, aggregating. | `etl`, `csv`, `parquet`, `ndjson`, `transform` |
| **data-validator** | stable | Autonomous data quality validation — schema inference and validation, completeness/uniqueness/consistency checks, statistical anomaly detection, time-series gap detection, referential integrity, dataset comparison and drift detection. Supports CSV, JSON, SQLite, APIs. Use after any data pipeline work. | `data-quality`, `schema-inference`, `anomaly-detection`, `drift`, `integrity` |
| **seed-data** | stable | Generate realistic test and seed data for any database schema: users, products, orders, time series, financial data. | `test-data`, `fixtures`, `timeseries`, `faker`, `schema` |
| **sql-query** | stable | Build and execute complex SQL queries for analytics: joins, window functions, CTEs, aggregations, pivots. | `sql`, `analytics`, `window-functions`, `cte`, `query-optimization` |
| **visualize** | stable | Generate charts and visualizations: line, bar, scatter, heatmap, candlestick. Output as PNG/SVG for Telegram or web. | `charts`, `matplotlib`, `candlestick`, `csv`, `plotting` |

## system

_OS/infra admin — services, backups, docker, logs, networking_ — 16 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **backup-restore** | stable | Automated backup strategies: database dumps, file snapshots, incremental backups, verification, disaster recovery. Use when: setting up backup schedules, creating database backups, restoring from b... | `backups`, `database-dumps`, `incremental`, `disaster-recovery`, `retention` |
| **cloud-deploy** | stable | Provision and manage cloud servers on Hetzner, DigitalOcean, or AWS. Set up infrastructure, firewalls, DNS, and deploy applications. | `hetzner`, `digitalocean`, `aws`, `vps-provisioning`, `dns` |
| **cron-dashboard** | stable | Unified dashboard for all cron jobs, systemd services, and Docker containers. Shows schedules, health status, log freshness, errors, upcoming runs, and diagnostics. Use when checking what's scheduled, what's running, what failed, or managing cron jobs. | `cron`, `systemd`, `docker`, `health-monitoring`, `diagnostics` |
| **docker** | stable | Manage Docker containers and Compose stacks: build, run, stop, logs, exec, prune, multi-service orchestration. | `containers`, `docker-compose`, `orchestration`, `images`, `volumes` |
| **error-monitor** | stable | Monitor application logs and stderr in real-time for errors, crashes, and exceptions. Auto-capture stack traces, alert on new error patterns, track error rates. | `log-monitoring`, `stack-traces`, `crash-reports`, `error-rates`, `stderr` |
| **file-manager** | beta | File operations — organize, search, backup, manage files and directories. Use for any file management task. | `file-operations`, `fd`, `ripgrep`, `trash`, `backup` |
| **health-check** | beta | Quick system health check — services, disk, memory, processes. Use for periodic health monitoring. | `disk`, `memory`, `systemd`, `load-average`, `monitoring` |
| **log-analyzer** | stable | Parse and analyze application logs: pattern matching, error extraction, correlation with timestamps, frequency analysis, anomaly detection. Use when: diagnosing production issues, finding error pat... | `log-parsing`, `error-aggregation`, `anomaly-detection`, `correlation`, `syslog` |
| **nginx-caddy** | stable | Configure Nginx or Caddy as a reverse proxy with SSL/TLS. Use when setting up reverse proxies, Let's Encrypt certificates, routing domains to backend services, load balancing, or serving static files. | `reverse-proxy`, `ssl`, `certbot`, `load-balancing`, `caddyfile` |
| **post-mortem** | stable | Generate structured post-mortems after incidents or failures: root cause analysis, timeline, impact assessment, prevention steps. | `incident-response`, `root-cause-analysis`, `timeline`, `blameless`, `retrospective` |
| **scheduler** | stable | Natural language task scheduling for Kite. Create, list, and cancel scheduled jobs in plain English with a human-readable registry of what's running and why. | `cron`, `crontab`, `scheduling`, `natural-language`, `registry` |
| **ssh-remote** | stable | SSH into remote servers, execute commands, transfer files via SCP/rsync, manage SSH keys and configs, tunnel ports. | `ssh`, `scp`, `rsync`, `tunnels`, `key-management` |
| **system-admin** | stable | System administration — manage services, processes, cron, systemd, networking. Use for any sysadmin task. | `systemd`, `cron`, `process-management`, `networking`, `journalctl` |
| **systemd-manager** | stable | Create and manage systemd services, timers, and socket units. Use when creating background services or daemons, setting up scheduled timers, debugging failed units, viewing unit logs, or managing service dependencies. | `systemd`, `timers`, `journalctl`, `unit-files`, `daemons` |
| **tmux** | stable | Remote-control tmux sessions for interactive CLIs by sending keystrokes and scraping pane output. | `tmux`, `session-control`, `keystrokes`, `pane-capture`, `automation` |
| **watchdog** | stable | Monitor system health — check services, disk usage, processes, logs. Use when asked about system status or health. | `systemd`, `disk-usage`, `process-monitoring`, `journalctl`, `health-check` |

## comms

_Person-to-person messaging — Slack, email, push, voice_ — 9 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **bluebubbles** | stable | Use when you need to send or manage iMessages via BlueBubbles (recommended iMessage integration). Calls go through the generic message tool with channel="bluebubbles". | `imessage`, `sms`, `tapback`, `group-chat`, `attachments` |
| **discord** | stable | Discord ops via the message tool (channel=discord). | `discord`, `messaging`, `channels`, `reactions`, `threads` |
| **email-manager** | stable | Read, search, send, and manage email via IMAP/SMTP or API (Gmail, Outlook). | `imap`, `smtp`, `gmail`, `email-send`, `inbox` |
| **himalaya** | stable | CLI to manage emails via IMAP/SMTP. Use `himalaya` to list, read, write, reply, forward, search, and organize emails from the terminal. Supports multiple accounts and message composition with MML (MIME Meta Language). | `email`, `imap`, `smtp`, `cli`, `mime-mml` |
| **imsg** | stable | iMessage/SMS CLI for listing chats, history, and sending messages via Messages.app. | `imessage`, `sms`, `macos`, `messages-app`, `cli` |
| **ntfy** | stable | Send push notifications to phones/desktops via ntfy. Use when the user wants to send alerts, reminders, or notifications. | `push-notifications`, `ntfy`, `alerts`, `curl`, `reminders` |
| **slack** | stable | Use when you need to control Slack from OpenClaw via the slack tool, including reacting to messages or pinning/unpinning items in Slack channels or DMs. | `slack`, `messaging`, `reactions`, `pins`, `bot-token` |
| **voice-call** | beta | Start voice calls via the OpenClaw voice-call plugin. | `voice`, `twilio`, `telnyx`, `phone-calls`, `plugin` |
| **wacli** | stable | Send WhatsApp messages to other people or search/sync WhatsApp history via the wacli CLI (not for normal user chats). | `whatsapp`, `messaging`, `wacli`, `history-search`, `send` |

## writing

_Authoring — docs, knowledge bases, ADRs, drafts, prompts_ — 6 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **adr-manager** | beta | Record and retrieve Architecture Decision Records — log a decision's context, options, rationale, and tradeoffs, then list/search them. Use when making a non-trivial design or architecture decision, choosing between tools/approaches, or asked 'why did we do it this way?' | `adr`, `decision-records`, `architecture-decisions`, `documentation` |
| **draft-queue** | beta | Queue an external action (Telegram message, shell command, GitHub comment, PR merge, file write) for the user's explicit approval before it runs. Use when you want to take an outward-facing or irreversible action that needs sign-off first. | `approval-queue`, `drafts`, `json-payload`, `telegram-action` |
| **knowledge-base** | beta | Ingest documents (URLs, local files, PDFs) into a searchable local knowledge base and query stored knowledge. Use when ingesting or storing documents, searching previously stored knowledge, or building research context. | `ingestion`, `sqlite-fts5`, `bm25`, `pdf`, `search` |
| **knowledge-graph** | stable | SQLite-backed entity-relationship store. Entities (people, projects, decisions, tools, rules) connected by typed relations (decided-by, depends-on, supersedes, etc.). Full-text search, path finding... | `entity-relationship`, `sqlite`, `fts5`, `path-finding`, `graph` |
| **latex-doc** | stable | Converts Markdown → LaTeX → PDF. Handles Mermaid diagrams, math, tables, citations, figures. | `latex`, `pdf`, `pandoc`, `mermaid`, `templates` |
| **prompt-library** | stable | Library of tested prompt patterns for sub-agent orchestration: task decomposition, verification, error recovery, chain-of-thought. | `prompt-patterns`, `sub-agent-orchestration`, `templates`, `task-decomposition`, `anti-patterns` |

## productivity

_Personal org — calendars, notes, tasks, scheduling, clipboards_ — 18 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **apple-notes** | stable | Manage Apple Notes via the `memo` CLI on macOS (create, view, edit, delete, search, move, and export notes). Use when a user asks OpenClaw to add a note, list notes, search notes, or manage note folders. | `apple-notes`, `memo-cli`, `macos`, `note-taking`, `export` |
| **apple-reminders** | stable | Manage Apple Reminders via remindctl CLI (list, add, edit, complete, delete). Supports lists, date filters, and JSON/plain output. | `apple-reminders`, `remindctl`, `macos`, `todos`, `due-dates` |
| **bear-notes** | stable | Create, search, and manage Bear notes via grizzly CLI. | `bear`, `grizzly-cli`, `macos`, `note-taking`, `tags` |
| **calendar-manager** | stable | Read and manage Google Calendar events: list upcoming, create events, check availability, send reminders. | `google-calendar`, `ics`, `scheduling`, `availability`, `reminders` |
| **event-log** | stable | An append-only structured JSONL event log giving Kite continuity across sessions — without reading every log file. | `jsonl`, `event-log`, `session-continuity`, `append-only`, `audit-trail` |
| **gog** | stable | Google Workspace CLI for Gmail, Calendar, Drive, Contacts, Sheets, and Docs. | `google-workspace`, `gmail`, `calendar`, `drive`, `sheets` |
| **kanban-agent** | stable | Persistent task board backed by SQLite. Survives across sessions. Supports priorities (p0-p3), statuses (backlog → todo → in-progress → review → done), deadlines, dependencies, projects, tags, and ... | `kanban`, `task-board`, `sqlite`, `priorities`, `standup` |
| **morning-briefing** | stable | Generate a morning briefing with weather, market overview, system health, and upcoming tasks. Use for daily briefings. | `daily-briefing`, `weather`, `system-health`, `github`, `telegram` |
| **notion** | stable | Notion API for creating and managing pages, databases, and blocks. | `notion-api`, `pages`, `databases`, `blocks`, `data-sources` |
| **obsidian** | stable | Work with Obsidian vaults (plain Markdown notes) and automate via obsidian-cli. | `obsidian`, `markdown-notes`, `obsidian-cli`, `vault`, `wikilinks` |
| **project-brief** | stable | Create and maintain one-page project briefs (goal, success criteria, constraints, stack, status, next actions). Use when starting a new project, updating a project's status, or asked 'what's the status of X?' | `project-status`, `brief`, `goals`, `blockers`, `tracking` |
| **project-index** | stable | Kite's project registry — a structured JSON index of all active projects. | `project-registry`, `status-tracking`, `json-index`, `cron`, `metadata` |
| **project-planner** | stable | Decompose a project into tasks with dependencies, critical path, and tracking before execution. Use at the start of every significant project before spawning sub-agents or writing code; also for project status, task failures, or changes affecting other tasks. | `task-planning`, `dependency-graph`, `subtasks`, `impact-analysis`, `orchestration` |
| **session-logs** | stable | Search and analyze your own session logs (older/parent conversations) using jq. | `jsonl`, `jq`, `conversation-history`, `transcript-search`, `cost-tracking` |
| **shared-clipboard** | stable | Cross-session key-value clipboard for sharing snippets, code, URLs, configs, and notes between agents, sessions, and devices. Use when storing, retrieving, or sharing text snippets, when piping data between commands, or when the user says "save this", "remember this snippet", "clipboard", "clip", or "scratchpad". | `clipboard`, `scratchpad`, `sqlite`, `snippets`, `cross-session` |
| **task-queue** | stable | Persistent JSON-backed job queue for scheduling shell commands without raw cron — supports one-shot (run at a time) and recurring (cron schedule) tasks. Use when asked to schedule or run a job at/every some interval, list or cancel queued tasks, or check task status. | `task-queue`, `scheduling`, `cron`, `pipelines`, `job-runner` |
| **things-mac** | stable | Manage Things 3 via the `things` CLI on macOS (add/update projects+todos via URL scheme; read/search/list from the local Things database). Use when a user asks OpenClaw to add a task to Things, list inbox/today/upcoming, search tasks, or inspect projects/areas/tags. | `things3`, `tasks`, `todos`, `macos`, `url-scheme` |
| **trello** | stable | Manage Trello boards, lists, and cards via the Trello REST API. | `trello`, `kanban`, `boards`, `cards`, `rest-api` |

## media

_Image/video/audio — generation, transcription, OCR, screenshots_ — 14 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **gifgrep** | stable | Search GIF providers with CLI/TUI, download results, and extract stills/sheets. | `gif-search`, `tenor`, `giphy`, `download`, `frame-extraction` |
| **media-fetch** | stable | Download video/audio from 1000+ sites (YouTube, Twitter/X, Spotify, TikTok, etc.), extract audio, download subtitles, transcribe speech to text, and search for videos. Use when asked to download, summarize, or transcribe any video/audio URL, or when asked to search YouTube. | `yt-dlp`, `ffmpeg`, `whisper`, `transcription`, `youtube` |
| **nano-banana-pro** | stable | Generate or edit images via Gemini 3 Pro Image (Nano Banana Pro). | `image-generation`, `gemini`, `image-editing`, `ai-images` |
| **nano-pdf** | beta | Edit PDFs with natural-language instructions using the nano-pdf CLI. | `pdf-editing`, `natural-language`, `cli`, `document-editing` |
| **ocr-document** | stable | Extracts text and tables from PDFs and images using PyMuPDF, pdfplumber, and Tesseract OCR (Norwegian/English), with auto/text/ocr/hybrid methods and page selection. Use when reading, OCR-ing, or extracting text/tables from a PDF, scanned document, or image. | `ocr`, `tesseract`, `pdf-extraction`, `pymupdf`, `norwegian` |
| **openai-image-gen** | stable | Batch-generate images via OpenAI Images API. Random prompt sampler + `index.html` gallery. | `image-generation`, `openai`, `dall-e`, `gpt-image`, `gallery` |
| **openai-whisper** | stable | Local speech-to-text with the Whisper CLI (no API key). | `whisper`, `transcription`, `speech-to-text`, `local`, `audio` |
| **openai-whisper-api** | stable | Transcribe audio via OpenAI Audio Transcriptions API (Whisper). | `whisper`, `transcription`, `openai-api`, `speech-to-text`, `audio` |
| **peekaboo** | stable | Capture and automate macOS UI with the Peekaboo CLI. | `macos`, `screenshot`, `ui-automation`, `screen-capture`, `accessibility` |
| **sag** | stable | ElevenLabs text-to-speech with mac-style say UX. | `tts`, `elevenlabs`, `audio`, `voice`, `speech-synthesis` |
| **sherpa-onnx-tts** | stable | Local text-to-speech via sherpa-onnx (offline, no cloud) | `tts`, `text-to-speech`, `offline`, `onnx`, `piper` |
| **songsee** | stable | Generate spectrograms and feature-panel visualizations from audio with the songsee CLI. | `audio`, `spectrogram`, `visualization`, `ffmpeg`, `cli` |
| **video-frames** | beta | Extract frames or short clips from videos using ffmpeg. | `ffmpeg`, `video`, `frame-extraction`, `thumbnails`, `clips` |
| **website-screenshot** | stable | Take a headless Chromium screenshot of any URL and send/save the result. Use when the user asks for a screenshot, snapshot, or visual of a website, webpage, or URL — including requests like "show me what X looks like", "capture the homepage of Y", or "screenshot this URL". Supports viewport vs full-page, mobile emulation, dark mode, and configurable wait strategies. | `playwright`, `chromium`, `headless`, `full-page`, `mobile-emulation` |

## trading-finance

_Markets & money — market data, TA, sentiment, ordering, prop trading_ — 11 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **economic-data-collector** | stable | Collects and queries macroeconomic data — yield curves, central bank policy rates, forex rate differentials, FRED US economic indicators (CPI, GDP, NFP, unemployment), and the economic calendar. Use when fetching, storing, or querying interest rates, bond yields, rate diffs, or economic releases for trading and forex analysis. | `fred`, `yield-curve`, `central-bank-rates`, `rate-differentials`, `forex` |
| **food-order** | beta | Reorder Foodora orders + track ETA/status with ordercli. Never confirm without explicit user approval. Triggers: order food, reorder, track ETA. | `ordercli`, `foodora`, `reorder`, `eta-tracking`, `commerce` |
| **fundamental-research-engine** | stable | Generates fundamental trading research per instrument (forex, metals, crypto, energy, indices) — rate differentials, COT positioning, retail sentiment, currency strength, central-bank rates, bond yields, Fear & Greed — plus an all-instrument daily market brief. Use when analyzing an instrument's fundamentals, checking rate differentials or COT/sentiment, or producing a daily macro brief. | `forex`, `cot`, `rate-differentials`, `instrument-analysis`, `daily-brief` |
| **macro-briefing** | stable | Collect macro market and economic-calendar data (yfinance + FairEconomy API) into a SQLite DB and generate a formatted macro briefing for Telegram. Use when producing a macro/markets briefing or collecting macro market data. | `yfinance`, `economic-calendar`, `yield-curve`, `vix`, `sqlite` |
| **market-data-engine** | stable | Collects and queries financial market data (OHLCV bars, price snapshots, currency strength rankings) for forex, currency indices, equity indices, metals, energy, and crypto via yfinance and TradingView into a SQLite database. Use when fetching prices, collecting/backfilling market or OHLCV data, computing currency strength, or querying stored quotes across timeframes (1m to weekly). | `forex`, `ohlcv`, `yfinance`, `currency-strength`, `sqlite` |
| **opportunity-scanner** | stable | Scans markets across technical, fundamental, macro, and sentiment engines to surface high-conviction trade opportunities with conviction scores and Telegram alerts. Use when asked to scan for opportunities, find trade setups, get a trade alert, decide what to trade, or schedule recurring pre-market opportunity scans via cron. | `trade-scanner`, `opportunity-scan`, `market-signals`, `conviction-score`, `cron` |
| **ordercli** | stable | Foodora-only CLI for checking past orders and active order status (Deliveroo WIP). | `foodora`, `food-delivery`, `ordering`, `commerce`, `order-tracking` |
| **research-synthesizer** | stable | Synthesizes technical, fundamental, macro, and sentiment signals into conviction-scored trade ideas across forex, stocks, and crypto. Use when scanning markets for opportunities, ranking trade ideas by conviction, deciding what to trade, or asking for a buy/sell signal or setup on an instrument. | `trade-ideas`, `conviction-scoring`, `technical-analysis`, `fundamentals`, `opportunity-scan` |
| **sector-rotation** | stable | Track equity sector rotation — collect sector ETF data and compute relative-strength heatmaps, sector leaders, and market cycle phase. Use when analyzing sector rotation, relative strength across sectors, or where we are in the market cycle. | `relative-strength`, `yfinance`, `business-cycle`, `money-flow`, `etf` |
| **sentiment-engine** | stable | Collects and queries market sentiment data — Myfxbook retail long/short forex positioning, CFTC COT institutional net positions, and crypto/equity Fear & Greed indices — for contrarian trading signals. Use when analyzing retail positioning, COT data, fear and greed, sentiment extremes, or contrarian setups for forex, metals, energy, or crypto. | `cot`, `myfxbook`, `fear-greed`, `retail-positioning`, `contrarian` |
| **technical-analysis-engine** | stable | Computes technical indicators (RSI, MACD, EMA, Bollinger Bands, ATR, Stochastic), scans forex/instruments for trade setups (momentum, reversal, squeeze, trend-pullback), and analyzes market structure (BOS/CHoCH, support/resistance, order blocks, fair value gaps, regime). Use when doing technical analysis, screening for setups, swing trades, multi-timeframe analysis, or checking indicators on a ticker. | `technical-analysis`, `indicators`, `forex`, `screener`, `market-structure` |

## home-iot

_Smart home & local devices — lights, speakers, weather, places_ — 8 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **blucli** | beta | BluOS CLI (blu) for discovery, playback, grouping, and volume. | `bluos`, `bluesound`, `audio-playback`, `speaker-control`, `tunein` |
| **camsnap** | beta | Capture frames or clips from RTSP/ONVIF cameras. | `rtsp`, `onvif`, `ip-camera`, `snapshot`, `ffmpeg` |
| **eightctl** | beta | Control Eight Sleep pods (status, temperature, alarms, schedules). | `eight-sleep`, `smart-bed`, `temperature`, `alarms`, `cli-wrapper` |
| **goplaces** | stable | Query Google Places API (New) via the goplaces CLI for text search, place details, resolve, and reviews. Use for human-friendly place lookup or JSON output for scripts. | `google-places`, `place-lookup`, `geosearch`, `reviews`, `location` |
| **openhue** | stable | Control Philips Hue lights and scenes via the OpenHue CLI. | `philips-hue`, `lights`, `scenes`, `smart-home`, `brightness` |
| **sonoscli** | beta | Control Sonos speakers (discover/status/play/volume/group). | `sonos`, `speakers`, `playback`, `local-network`, `ssdp` |
| **spotify-player** | beta | Terminal Spotify playback/search via spogo (preferred) or spotify_player. | `spotify`, `playback`, `music`, `spogo`, `cli` |
| **weather** | beta | Get current weather and forecasts. Use when the user asks about weather, temperature, or forecasts for any location. | `wttr`, `forecast`, `curl`, `oslo`, `temperature` |

## meta

_Self-tooling — skill/model management, MCP, LLM CLIs_ — 17 skill(s)

| Skill | Maturity | Description | Tags |
|-------|----------|-------------|------|
| **budget** | beta | Estimate and rank what is consuming the context window — CLAUDE.md, MEMORY.md, USER.md, the 162 auto-loaded skill descriptions (ranked heaviest-first), and tool/MCP schemas. Use when asked about context bloat, token weight, "what's eating my context", which skills are heaviest, or with --cost for real spend. | `context-window`, `tokens`, `budget`, `bloat`, `skill-descriptions`, `cost` |
| **canvas** | stable | Display HTML content on connected OpenClaw nodes (Mac app, iOS, Android). | `html-display`, `openclaw-nodes`, `webview`, `live-reload`, `screenshot` |
| **clawhub** | stable | Use the ClawHub CLI to search, install, update, and publish agent skills from clawhub.com. Use when you need to fetch new skills on the fly, sync installed skills to latest or a specific version, or publish new/updated skill folders with the npm-installed clawhub CLI. | `skill-registry`, `skill-install`, `skill-publish`, `marketplace`, `versioning` |
| **cost-optimizer** | beta | Track and optimize LLM token spend: estimate costs per task, recommend model tiers, monitor cumulative usage, suggest batching strategies. | `token-counting`, `llm-pricing`, `model-tier`, `usage-analysis`, `budget-estimation` |
| **gemini** | beta | Gemini CLI for one-shot Q&A, summaries, and generation. | `gemini-cli`, `llm`, `one-shot`, `google-ai`, `generation` |
| **mcp-hub** | stable | MCP server integration hub — standardized tool access to databases, filesystems, memory, reasoning, and 100s of third-party services via Model Context Protocol. Use when querying databases, structured file ops, knowledge graph memory, step-by-step reasoning, or when the user asks about MCP servers. | `mcp`, `model-context-protocol`, `mcporter`, `knowledge-graph`, `filesystem` |
| **mcporter** | stable | Use the mcporter CLI to list, configure, auth, and call MCP servers/tools directly (HTTP or stdio), including ad-hoc servers, config edits, and CLI/type generation. | `mcp`, `cli`, `stdio`, `oauth`, `codegen` |
| **memory-search** | stable | Semantic memory search across all agent memories and conversation history. Use BEFORE answering questions about prior work, decisions, dates, people, preferences, projects, or past conversations. Also use when asked "do you remember", "what did we discuss", "when did we", etc. | `semantic-search`, `rag`, `embeddings`, `cross-agent`, `vector` |
| **meta-analyst** | stable | Self-reflection and auto-improvement skill — analyzes session logs, tracks patterns, produces efficiency reports | `session-analysis`, `self-reflection`, `efficiency-report`, `event-tracking`, `patterns` |
| **model-router** | stable | Classify a task and pick the right Claude model tier (haiku/sonnet/opus) before significant work. Use every time before spawning a sub-agent or starting substantial work, to right-size the model to the task. | `model-routing`, `tier-selection`, `cost`, `classifier`, `subagents` |
| **model-usage** | beta | Use CodexBar CLI local cost usage to summarize per-model usage for Codex or Claude, including the current (most recent) model or a full model breakdown. Trigger when asked for model-level usage/cost data from codexbar, or when you need a scriptable per-model summary from codexbar cost JSON. | `codexbar`, `cost`, `per-model-usage`, `cli`, `macos` |
| **oracle** | stable | Best practices for using the oracle CLI (prompt + file bundling, engines, sessions, and file attachment patterns). | `llm-cli`, `gpt-5`, `one-shot`, `file-bundling`, `sessions` |
| **self-skill** | stable | Meta-skill for detecting when a solved problem is worth packaging as a reusable skill, then scaffolding it. Runs automatically after completing any non-trivial task. Also use when explicitly asked to create, draft, or propose a new skill from something just built. | `skill-scaffolding`, `self-tooling`, `heuristics`, `draft-skill`, `automation` |
| **skill-creator** | stable | Create or update AgentSkills. Use when designing, structuring, or packaging skills with scripts, references, and assets. | `agentskills`, `scaffolding`, `progressive-disclosure`, `packaging`, `skill-design` |
| **skill-index** | beta | Query Claudex's own skill catalog by category, tag, or capability. Use when asked "what skills exist for X", "do we have a skill for Y", "list all backend skills", or when planning multi-step work that might benefit from a skill you didn't load. | `skill-catalog`, `jq`, `discovery`, `self-tooling`, `frontmatter` |
| **webhook-receiver** | stable | Lightweight background HTTP server that receives webhooks from external services (GitHub, ntfy, Stripe, any HTTP POST) and runs configured shell commands in response. Use when setting up webhook integrations, receiving GitHub push/PR/issue events, triggering scripts from external services, or exposing a local endpoint for testing. Switches from polling to event-driven: things happen the moment they're triggered, not on the next cron cycle. | `http-server`, `hmac-sha256`, `github-events`, `event-driven`, `node` |
| **whoami** | beta | Show the auto-curated user profile (memory/USER.md) — who the user is, preferences, working patterns, recent corrections, and open threads — with last-updated, section counts, and a token-cap/prune check. Use when asked "what do you know about me", "show my profile", "whoami", or to review/prune the profile. | `user-profile`, `memory`, `self-knowledge`, `review`, `curation` |

---

Legend: 🔒 = private (workspace-only)  ·  ⚙️ = has external project dependency (see the skill's `INSTALL.md`)
